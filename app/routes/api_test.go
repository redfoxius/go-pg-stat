package routes

import (
	"testing"
)

func TestSetup(t *testing.T) {
	//routeName := "/api/stat/get"
	//
	//app := fiber.New()
	//
	//Setup(app)
	//
	//testRoute := app.GetRoute(routeName)
	//
	//assert.Equal(t, testRoute, nil)
}
